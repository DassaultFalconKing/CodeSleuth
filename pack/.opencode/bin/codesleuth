#!/usr/bin/env sh
set -eu
HERE=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
ROOT=$(CDPATH= cd -- "$HERE/../.." && pwd)
export CODESLEUTH_TARGET_ROOT="$ROOT"
exec python3 "$HERE/codesleuth_tui_bootstrap.py" --target "$ROOT" "$@"
