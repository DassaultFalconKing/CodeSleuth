name: W7 TDD witness

on:
  push:
    branches:
      - feature/rc7-acceptance-profile-snapshot

permissions:
  contents: read

jobs:
  focused:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - name: W7 focused regression
        run: bun tests/acceptance_profile_snapshot_smoke.ts
      - name: Package exact local harness
        if: always()
        run: |
          git rev-parse HEAD > W7-EXACT-SHA.txt
          git archive --format=zip --output=w7-source.zip HEAD
          cp "$(command -v bun)" w7-bun
          tar -czf w7-node-modules.tar.gz node_modules
      - name: Upload exact local harness
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: w7-harness-${{ github.sha }}
          path: |
            w7-source.zip
            W7-EXACT-SHA.txt
            w7-bun
            w7-node-modules.tar.gz
          if-no-files-found: error
