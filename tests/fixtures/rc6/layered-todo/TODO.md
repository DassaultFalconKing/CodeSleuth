# Planning Authority

This file is the only planning source of truth.
Current prerequisite stop-gate: `docs/SECURITY-STOP.md`.
`docs/ROADMAP-OLD.md` is superseded and must not be resumed.
`docs/CURRENT-STATE.md` is supporting current-state evidence only.
`docs/archive/SHIPPED.md` is historical shipped work, not an active plan.
